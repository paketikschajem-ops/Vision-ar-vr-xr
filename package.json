{
  "name": "vision-ar-vr-xr",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'no build step'",
    "cap:sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  }
}
